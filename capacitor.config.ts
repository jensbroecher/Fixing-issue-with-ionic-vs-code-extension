import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'afridelivery.ionic.app.2023',
  appName: 'afridelivery-ionic-app-2023',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
